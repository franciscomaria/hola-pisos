import { SeparatorImagesPdfPipe } from './separator-images-pdf.pipe';

describe('SeparatorImagesPdfPipe', () => {
  it('create an instance', () => {
    const pipe = new SeparatorImagesPdfPipe();
    expect(pipe).toBeTruthy();
  });
});
